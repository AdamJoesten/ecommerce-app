// prettier.config.ts, .prettierrc.ts, prettier.config.mts, or .prettierrc.mts

import { type Config } from "prettier";
import { baseConfig } from "@ecommerce/prettier-config"

export const config: Config = {
    ...baseConfig
};

